import { h } from 'vue';
import IconBase  from '../component/IconBase'
import { <%= name %> as iconDef } from '@xunserver/icon'

export const <%= name %> = () => {
  return h(IconBase, { icon: iconDef.icon });
}

export default {
  install(app) {
    app.component('<%= name %>Icon', <%= name %>)
  }
}
