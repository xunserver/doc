{
  "name": "@xunserver/icon",
  "version": "0.0.5",
  "description": "",
  "main": "index.js",
  "scripts": {
    "g": "npm run generate",
    "generate": "cross-env NODE_ENV=production gulp -LLLL",
    "clean": "gulp clean",
    "build:es": "cross-env NODE_ENV=production tsc --project tsconfig.build.json --module esnext --outDir dist/es",
    "build:lib": "cross-env NODE_ENV=production tsc --project tsconfig.build.json --module CommonJS --outDir dist/lib",
    "build": "npm run g && npm run build:es && npm run build:lib",
    "pub": "npm run build && node publish.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rgrove/parse-xml": "^3.0.0",
    "@types/ejs": "^3.1.1",
    "@types/lodash": "^4.14.185",
    "@types/through2": "^2.0.36",
    "@types/vinyl": "^2.0.6",
    "del": "^5.0.0",
    "ejs": "^3.1.8",
    "lodash": "^4.17.21",
    "svgo": "^2.8.0",
    "through2": "^3.0.2"
  },
  "dependencies": {
    "@types/gulp-rename": "^2.0.1",
    "@types/svgo": "^2.6.4",
    "@xunserver/util": "workspace:*",
    "gulp-rename": "^2.0.0"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
